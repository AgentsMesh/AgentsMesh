# Traefik Static Configuration for Development
# Auto-generated by dev.sh - project: agentsmesh-worktree-1-25-f0255143

api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
  grpc:
    address: ":9443"

providers:
  docker:
    exposedByDefault: false
    network: agentsmesh-worktree-1-25-f0255143_default
    # Only discover containers with this project's label
    constraints: "Label(`com.docker.compose.project`, `agentsmesh-worktree-1-25-f0255143`)"
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: DEBUG
  format: common

accessLog:
  format: common
