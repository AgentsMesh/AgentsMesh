# Traefik Static Configuration for Development
# Auto-generated by dev.sh - project: agentsmesh-fix-repository-pod

api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
  grpc:
    address: ":9443"

providers:
  docker:
    exposedByDefault: false
    network: agentsmesh-fix-repository-pod_default
    # Only discover containers with this project's label
    constraints: "Label(`com.docker.compose.project`, `agentsmesh-fix-repository-pod`)"
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: DEBUG
  format: common

accessLog:
  format: common
